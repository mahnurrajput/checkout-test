export const metadata = {
  title: "Bint-e-Khalil Art — Checkout Test",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ fontFamily: "system-ui, sans-serif", margin: 0, background: "#faf7f2" }}>
        {children}
      </body>
    </html>
  );
}
